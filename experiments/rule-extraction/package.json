{
  "name": "@hippokampus/rule-extraction",
  "version": "0.1.0",
  "private": true,
  "description": "Experiment: can Claude extract business rules from approved code changes with useful precision?",
  "type": "module",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "cli": "tsx src/cli.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@vitest/coverage-v8": "^5.0.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
